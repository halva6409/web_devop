# 📖 ГЛАВА 2: ФРОНТЕНД - СОЗДАНИЕ КРАСИВЫХ СТРАНИЦ

## 🎯 Цель этой главы
Ты поймёшь:
- Как устроен HTML (структура страницы)
- Как работает CSS (красота и стиль)
- Как Flask и HTML работают вместе
- Как использовать Jinja2 для динамических страниц

---

## 🏗️ ЧАСТЬ 1: HTML - СКЕЛЕТ СТРАНИЦЫ

### Аналогия с домом

```
HTML  = Скелет дома (кирпичи, стены, элементы конструкции)
CSS   = Оформление (краска, обои, мебель)
JS    = Жизнь в доме (электричество, вода, люди)
```

### Базовая структура HTML

```html
<!DOCTYPE html>                    <!-- Сообщаем что это HTML5 -->
<html>                             <!-- Начало документа -->
  <head>                           <!-- Техническая информация -->
    <title>Название в вкладке</title>
    <meta charset="UTF-8">         <!-- Кодировка (поддержка русского) -->
  </head>
  <body>                           <!-- Видимое содержимое -->
    <h1>Главный заголовок</h1>
    <p>Текст параграфа</p>
  </body>
</html>
```

### Основные теги HTML

```html
<!-- Заголовки (от самого важного h1 до h6) -->
<h1>Главный заголовок</h1>      <!-- Самый большой -->
<h2>Подзаголовок</h2>
<h3>Ещё меньше</h3>
<h6>Очень маленький</h6>         <!-- Самый маленький -->

<!-- Параграфы текста -->
<p>Это текст параграфа. Браузер автоматически добавит отступы.</p>

<!-- Ссылки -->
<a href="http://google.com">Ссылка на Google</a>
<a href="/about">Ссылка внутри сайта</a>

<!-- Изображения -->
<img src="photo.jpg" alt="Описание">
<img src="/uploads/image.png" alt="Загруженное изображение">

<!-- Кнопка -->
<button>Нажми меня</button>
<button onclick="alert('Привет!')">Интерактивная кнопка</button>

<!-- Форма (для ввода данных) -->
<form>
  <input type="text" placeholder="Введи текст">
  <input type="email" placeholder="Email">
  <textarea placeholder="Длинный текст"></textarea>
  <button type="submit">Отправить</button>
</form>

<!-- Контейнеры (ящики для группировки) -->
<div>Это блок для группировки</div>
<span>Это встроенный элемент</span>

<!-- Список -->
<ul>
  <li>Элемент 1</li>
  <li>Элемент 2</li>
  <li>Элемент 3</li>
</ul>
```

### Атрибуты (свойства тегов)

```html
<!-- id - уникальный идентификатор -->
<h1 id="main-title">Заголовок</h1>

<!-- class - для применения стилей -->
<div class="card">Карточка</div>
<div class="card">Ещё карточка</div>

<!-- data-* - для хранения информации -->
<div data-id="123" data-name="BMW">Автомобиль</div>

<!-- Все вместе -->
<button id="submit-btn" class="primary-btn" data-action="add">Добавить</button>
```

---

## 🎨 ЧАСТЬ 2: CSS - КРАСОТА И СТИЛЬ

### Как подключить CSS

**Способ 1: Внутри HTML**
```html
<style>
  p { color: blue; }  /* Все параграфы будут синие */
</style>
```

**Способ 2: Отдельный файл (ПРАВИЛЬНЫЙ СПОСОБ)**
```html
<link rel="stylesheet" href="style.css">
```

### Селекторы CSS

```css
/* По тегу (все параграфы) */
p {
  color: blue;
  font-size: 16px;
}

/* По ID (элемент с id="main-title") */
#main-title {
  color: red;
  font-size: 48px;
}

/* По класу (все элементы с class="card") */
.card {
  background: white;
  padding: 20px;
}

/* Множественный выбор */
h1, h2, h3 {
  font-family: Arial;
}

/* По атрибуту */
input[type="text"] {
  border: 1px solid gray;
}

/* Комбинация */
.card p {           /* p внутри .card */
  font-size: 14px;
}

.card:hover {       /* .card когда наводишься мышкой */
  box-shadow: 0 4px 8px rgba(0,0,0,0.2);
  transform: translateY(-5px);
}
```

### Основные свойства CSS

```css
/* Цвета и текст */
color: #ff0000;                /* Цвет текста (красный) */
background: #f0f0f0;           /* Цвет фона */
font-size: 16px;               /* Размер шрифта */
font-family: Arial, sans-serif; /* Семейство шрифтов */
font-weight: bold;             /* Жирность (normal, bold) */
text-align: center;            /* Выравнивание текста */

/* Отступы (пустое место) */
margin: 20px;                  /* Отступ СНАРУЖИ (расстояние от других) */
margin-top: 10px;              /* Отступ сверху */
padding: 20px;                 /* Отступ ВНУТРИ (внутри элемента) */

/* Размеры */
width: 300px;                  /* Ширина */
height: 200px;                 /* Высота */
max-width: 100%;               /* Максимальная ширина */

/* Границы и тени */
border: 2px solid black;       /* Граница */
border-radius: 12px;           /* Закруглённые углы */
box-shadow: 0 4px 8px rgba(0,0,0,0.2);  /* Тень */

/* Расположение */
display: flex;                 /* Гибкое расположение элементов */
justify-content: center;       /* Центрировать по горизонтали */
align-items: center;           /* Центрировать по вертикали */
gap: 20px;                     /* Расстояние между элементами */

/* Позиционирование */
position: absolute;            /* Абсолютное позиционирование */
top: 10px;                     /* От верхнего края */
left: 20px;                    /* От левого края */
z-index: 10;                   /* Слой (какой сверху) */
```

### Пример: Красивая карточка

```css
.product-card {
  width: 300px;
  padding: 20px;
  background: white;
  border-radius: 12px;
  box-shadow: 0 4px 8px rgba(0,0,0,0.1);
  transition: 0.3s;  /* Плавные изменения */
  cursor: pointer;
}

.product-card:hover {
  box-shadow: 0 8px 16px rgba(0,0,0,0.2);
  transform: translateY(-5px);  /* Приподнять на 5px */
}

.product-card h3 {
  color: #333;
  margin-bottom: 10px;
}

.product-card .price {
  color: #27ae60;
  font-size: 24px;
  font-weight: bold;
}

.product-card button {
  width: 100%;
  padding: 10px;
  background: #3498db;
  color: white;
  border: none;
  border-radius: 6px;
  cursor: pointer;
}

.product-card button:hover {
  background: #2980b9;
}
```

---

## 🔗 ЧАСТЬ 3: FLASK И HTML ВМЕСТЕ

### Шаблоны (Templates)

В Flask используются шаблоны - это HTML файлы которые могут содержать Python код!

Структура папок:
```
my_project/
├── app.py
├── templates/          ← Flask ищет сюда
│   └── index.html
└── static/
    └── css/
        └── style.css
```

### Передача данных из Flask в HTML

**app.py:**
```python
from flask import Flask, render_template

app = Flask(__name__,
            template_folder='templates',
            static_folder='static')

@app.route('/')
def index():
    cars = [
        {"name": "BMW", "price": 100000},
        {"name": "Mercedes", "price": 120000}
    ]
    message = "Добро пожаловать!"
    
    # Передаём переменные в шаблон
    return render_template('index.html', 
                         cars=cars,
                         message=message)
```

**templates/index.html:**
```html
<!DOCTYPE html>
<html>
<head>
    <title>Каталог авто</title>
    <!-- Правильный способ подключить CSS -->
    <link rel="stylesheet" href="{{ url_for('static', filename='css/style.css') }}">
</head>
<body>
    <h1>{{ message }}</h1>
    
    <!-- Цикл Jinja2 (это язык шаблонов Flask) -->
    {% for car in cars %}
        <div class="car-card">
            <h3>{{ car.name }}</h3>
            <p class="price">${{ car.price }}</p>
        </div>
    {% endfor %}
</body>
</html>
```

### Jinja2 - язык шаблонов Flask

**Переменные:**
```html
<!-- Простая переменная -->
<p>{{ name }}</p>

<!-- Из словаря -->
<p>{{ user['email'] }}</p>

<!-- Из объекта -->
<p>{{ product.price }}</p>

<!-- С фильтрами -->
<p>{{ text | upper }}</p>              <!-- В верхний регистр -->
<p>{{ price | round }}</p>             <!-- Округлить -->
<p>{{ date | strftime('%d.%m.%Y') }}</p>  <!-- Форматировать дату -->
```

**Условия:**
```html
{% if user.is_admin %}
    <button>Админ-панель</button>
{% elif user.is_moderator %}
    <button>Модерация</button>
{% else %}
    <p>Обычный пользователь</p>
{% endif %}
```

**Циклы:**
```html
<!-- Обычный цикл -->
{% for item in items %}
    <div>{{ item }}</div>
{% endfor %}

<!-- С индексом (начинается с 0) -->
{% for index, item in enumerate(items) %}
    <div>Элемент {{ index + 1 }}: {{ item }}</div>
{% endfor %}

<!-- Если список пуст -->
{% for item in items %}
    <div>{{ item }}</div>
{% else %}
    <p>Элементы не найдены</p>
{% endfor %}
```

---

## 📐 ЧАСТЬ 4: LAYOUT И ПОЗИЦИОНИРОВАНИЕ

### Flexbox - современный способ расулспределить элементы

```css
.container {
    display: flex;         /* Включить flex режим */
    justify-content: center;    /* Центрировать элементы по горизонтали */
    align-items: center;        /* Центрировать по вертикали */
    gap: 20px;                  /* Расстояние между элементами */
    flex-direction: row;        /* row или column */
    flex-wrap: wrap;            /* Переносить элементы если не влезли */
}
```

**Пример: Центрированные карточки**
```html
<!-- HTML -->
<div class="cards-container">
    <div class="card">Карточка 1</div>
    <div class="card">Карточка 2</div>
    <div class="card">Карточка 3</div>
</div>

<!-- CSS -->
<style>
.cards-container {
    display: flex;
    justify-content: center;
    gap: 20px;
    flex-wrap: wrap;
}

.card {
    width: 300px;
    padding: 20px;
    background: white;
    border-radius: 12px;
    box-shadow: 0 4px 8px rgba(0,0,0,0.1);
}
</style>
```

### Grid - для сложных макетов

```css
.grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);  /* 3 колонки одинакового размера */
    gap: 20px;
}

/* Что произойдёт при уменьшении экрана */
@media (max-width: 768px) {
    .grid {
        grid-template-columns: repeat(2, 1fr);  /* 2 колонки */
    }
}

@media (max-width: 480px) {
    .grid {
        grid-template-columns: 1fr;  /* 1 колонка */
    }
}
```

---

## 📱 ЧАСТЬ 5: АДАПТИВНЫЙ ДИЗАЙН

### Что такое адаптивный дизайн?

Сайт выглядит хорошо на:
- 📺 Больших экранах (компьютер)
- 💻 Средних экранах (планшет)
- 📱 Маленьких экранах (телефон)

### Meta тег для адаптивности

```html
<meta name="viewport" content="width=device-width, initial-scale=1.0">
```

Это говорит браузеру: "Используй ширину устройства и не уменьшай шрифт"

### Media Queries

```css
/* По умолчанию - для больших экранов */
.card {
    width: 300px;
    padding: 20px;
}

/* Если экран меньше 1024px (планшет) */
@media (max-width: 1024px) {
    .card {
        width: 250px;
    }
}

/* Если экран меньше 768px (маленький планшет) */
@media (max-width: 768px) {
    .card {
        width: 100%;
        padding: 15px;
    }
}

/* Если экран меньше 480px (телефон) */
@media (max-width: 480px) {
    .card {
        padding: 10px;
    }
}
```

---

## 🎓 ИТОГИ

После этой главы ты знаешь:
- ✅ Структуру HTML документа
- ✅ Основные HTML теги
- ✅ Как применять CSS стили
- ✅ Как работает Jinja2 в Flask
- ✅ Flexbox и Grid для размещения элементов
- ✅ Как сделать сайт адаптивным для всех экранов

**Вечером на практике ты:**
- Создашь HTML шаблоны
- Передашь данные из Flask в HTML
- Покрасишь страницу красивыми стилями
- Сделаешь адаптивный дизайн

---

**Помни:** Хороший дизайн это 50% успеха приложения! 🎨
