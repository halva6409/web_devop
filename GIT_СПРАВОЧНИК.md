# 📖 GIT СПРАВОЧНИК: КОМАНДЫ И ПРИМЕРЫ

## 🎯 Что такое Git?

Git - это система управления версиями. Представь что это как "Time Machine" для твоего кода:
- Сохраняешь версии кода
- Можешь вернуться в любую предыдущую версию
- Можешь видеть что изменилось
- Можешь работать в команде

---

## 📋 ОСНОВНЫЕ КОМАНДЫ

### 1️⃣ ИНИЦИАЛИЗАЦИЯ И НАСТРОЙКА

```bash
# Проверить установлена ли Git
git --version

# Первый раз - настроить своё имя и email
git config --global user.name "Иван"
git config --global user.email "ivan@gmail.com"

# Проверить настройки
git config --list

# Инициализировать Git в папке
git init
```

---

### 2️⃣ ОСНОВНОЙ РАБОЧИЙ ЦИКЛ

```bash
# Смотреть статус (что изменилось)
git status

# Добавить файл в "индекс" (準備 к сохранению)
git add file.py

# Добавить ВСЕ изменённые файлы
git add .

# Сохранить (создать "точку сохранения")
git commit -m "Описание что я сделал"

# Пример хорошего сообщения:
git commit -m "добавил функцию для загрузки файлов"

# Пример плохого:
git commit -m "fixes"  # Плохо - непонятно что изменилось
```

**Как работает:**
```
Ты пишешь код
   ↓
git status (видишь изменения)
   ↓
git add . (добавляешь в "черновик")
   ↓
git commit (сохраняешь "точку сохранения")
```

---

### 3️⃣ ПРОСМОТР ИСТОРИИ

```bash
# Смотреть историю всех commit'ов
git log

# Читаемый формат
git log --oneline

# Красивый график
git log --graph --oneline --all

# История одного файла
git log file.py

# Что изменилось в последнем commit
git show

# Разница между версиями
git diff
git diff commit1 commit2
```

---

### 4️⃣ ОТМЕНА ИЗМЕНЕНИЙ

```bash
# Отменить изменения в файле (вернуть как было)
git checkout file.py

# Отменить ВСЕ незасохраненные изменения
git checkout .

# Вернуться к старому commit
git revert commit_id

# Вернуться к commit ДО последнего (но сохранить изменения)
git reset --soft HEAD~1

# Вернуться к commit ДО последнего (и потерять изменения)
git reset --hard HEAD~1
```

---

### 5️⃣ РАБОТА С ВЕТКАМИ (Branches)

Ветки - как копии проекта где можно делать свои изменения не мешая основному коду.

```bash
# Создать новую ветку
git branch my-feature

# Переключиться на ветку
git checkout my-feature

# Создать и сразу переключиться
git checkout -b my-feature

# На современном Git
git switch -c my-feature

# Смотреть все ветки
git branch

# Смотреть ветки на сервере
git branch -a

# Удалить ветку (локально)
git branch -d my-feature

# Удалить ветку на сервере
git push origin -d my-feature
```

**Пример использования:**
```bash
# Ты работаешь на main
git switch -c add-login    # Создаёшь новую ветку для входа

# Делаешь свои изменения в add-login
git add .
git commit -m "добавил функцию входа"

# Когда готово - возвращаешься на main
git switch main

# И объединяешь ветки
git merge add-login
```

---

### 6️⃣ ОБЪЕДИНЕНИЕ ВЕТОК (Merge)

```bash
# Находишься на main ветке
git switch main

# Объединяешь в неё другую ветку
git merge add-login

# Если конфликт - нужно выбрать какой код оставить
git add .
git commit -m "merge: объединил add-login в main"
```

---

### 7️⃣ РАБОТА С GITHUB (Удалённый сервер)

```bash
# Скопировать проект с GitHub
git clone https://github.com/user/repo.git

# Смотреть связанные репозитории
git remote -v

# Добавить удалённый репозиторий
git remote add origin https://github.com/user/repo.git

# Загрузить свои commit на GitHub
git push origin main

# Скачать изменения с GitHub
git pull

# Это то же что и:
git fetch       # Скачать
git merge       # Объединить

# Загрузить новую ветку на GitHub
git push origin my-feature
```

---

### 8️⃣ ВАЖНЫЕ ПРАКТИКИ

```bash
# ВСЕГДА перед началом работы
git pull                    # Скачиваешь последние изменения

# Перед тем как push
git status                  # Проверяешь что изменилось
git diff                    # Смотришь разницу
git log --oneline -5        # Смотришь последние 5 commit

# Хороший workflow:
git switch -c feature-name  # Создашь ветку
# ... писал код ...
git add .
git commit -m "добавил фичу"
git push origin feature-name

# На GitHub создаёшь Pull Request
# Коллега проверяет твой код
# Объединяешь в main
git switch main
git pull
git merge feature-name
git push
```

---

## 🚨 ШПАРГАЛКА ДЛЯ БЫСТРОГО КОПИРОВАНИЯ

```bash
# Полный цикл сохранения
git status
git add .
git commit -m "описание"
git push

# Если что-то сломалось - откат на 1 шаг назад
git reset --soft HEAD~1
git checkout .

# Полный откат к последнему commit
git reset --hard HEAD~1

# Смотреть что сломалось
git log --oneline -10
git diff HEAD~1
```

---

## ❓ ЧАСТО ЗАДАВАЕМЫЕ ВОПРОСЫ

**Q: Как узнать что произошло в последнем commit?**
```bash
git show
```

**Q: Как восстановить удалённый файл?**
```bash
git log -- path/to/file       # Найти commit где был файл
git checkout commit_id -- path/to/file   # Восстановить
```

**Q: Как изменить сообщение в последнем commit?**
```bash
git commit --amend -m "новое сообщение"
```

**Q: Как убрать файл из commit но не удалять его?**
```bash
git reset --mixed HEAD~1  # Откатить на 1 шаг
# Выбрать какие файлы добавить
git add file1.py
git commit -m "новый commit"
```

**Q: Что делать если неправильно слился код (merge conflict)?**
```bash
git status  # Смотрим конфликты
# Открываем файлы и вручную выбираем нужный код
git add .
git commit -m "resolved conflicts"
```

---

## 📚 ССЫЛКИ

- Официальный сайт Git: https://git-scm.com
- GitHub: https://github.com
- Интерактивный обучении: https://learngitbranching.js.org

---

**Помни:** Git спасает жизни разработчика! 🚀
